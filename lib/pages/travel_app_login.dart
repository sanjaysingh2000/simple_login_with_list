import 'package:flutter/material.dart';

class TravelLogin extends StatelessWidget {
  const TravelLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
